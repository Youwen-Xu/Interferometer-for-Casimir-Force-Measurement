# 卡西米尔力测量：纳米位移台控制

这是一个适配 NATORS `NTControl.dll` 的 64 位 Windows 图形化控制程序。当前版本包含三种运动方式：

- 长按“后退 / 前进”箭头，程序按时间连续累计纳米级闭环目标；松开鼠标、窗口失去焦点或点击“停止运动”都会停止。
- 指定方向、位移（nm）和时间（s），程序按给定时间生成闭环纳米目标轨迹。例如 `100 nm / 5 s = 20 nm/s`。
- 指定振幅（nm）、频率（Hz）和持续时间（s），程序以启动位置为中心生成 `x(t) = A·sin(2πft)` 的闭环周期轨迹。振幅是峰值，因此峰峰值为 `2A`；到达持续时间或点击“停止”时结束。
- 以 `10 Hz` 从 Ametek 7270 的 `/data.shtml` 读取双通道数据；设备 IP 可在窗口中输入，默认是 `169.254.1.100`。程序实时显示 `X/Y/R/θ`、`R1/R2` 和展开后的连续干涉位移，并在一个放大的绘图区中分页显示三张最近 200 点的滚动曲线。

## 运行

双击 `NanoStageControl.exe` 或 `启动控制程序.bat`。

程序与 64 位 `NTControl.dll` 必须放在同一文件夹。默认设备定位符来自已有实验代码：`usb:id:2045392679`，默认通道为 `0`。

## 操作说明

1. 仪器接好并上电后，确认定位符与通道，再点击“连接”。
2. 连接时程序会自动启用位置传感器。设备连接期间，即使位移台没有运动，窗口底部也会每 `100 ms` 刷新一次实测位置；运动期间则由运动线程连续刷新。长按速度单位为 `nm/s`；例如 `20 nm/s` 时，软件约每 50 ms 累计 `1 nm` 的闭环相对目标。
3. 定时模式按照指定时间逐步累计目标，到时立即停止，并同时显示指令位移、传感器测得位移和实际用时。
4. 正弦模式从启动位置的零相位开始，先向正方向运动，再围绕启动位置往复振动。可设频率范围为 `0.001–10 Hz`；窗口会同步显示峰峰值、峰值速度和计划周期数。若持续时间不是整数周期，运动会在对应的正弦相位处停止。
5. 任何运动过程中都可以点击相应区域的“停止”或“停止运动”。
6. Ametek 区域中的 `7270 IP`、`k` 和 `λ` 都可在开始采集前修改；默认值分别为 `169.254.1.100`、`1` 和 `632.8 nm`。IP 栏只填设备的 IP 地址或主机名，不要带 `http://`、端口或路径。点击“开始采集”后这些输入会暂时锁定，并按 10 Hz 更新数值与曲线；停止后可保存包含全部样本的 CSV 文件。
7. 窗口底部只显示一张放大的曲线图，右侧可手动选择第 `1 / 2 / 3` 页，依次查看 `R1/R2 幅值`、`R1/R2 比值`和`连续干涉位移`。切换页面不会清除已采集数据。
8. 位移页右侧的“反转”键会立即翻转连续位移读数和整条位移曲线，但不会逐点改写内存中的采样数据。每按一次切换一次方向；停止采集并保存 CSV 时，程序按照最终反转状态统一写入全部位移值。开始一轮新采集时反转状态复位为“关”。

窗口会明确区分“位移台编码器位置”和“Ametek 干涉位移”。每个样本先按 `arccot(R1/R2/k)` 得到折叠相位；当相位趋势反向时，程序比较转折值更靠近 `0` 还是 `π/2`，据此切换到相邻区间并连续展开，再按 `λ/(4π)` 换算为纳米位移。远离边界的趋势反向仍按位移台真实换向处理。由于双通道幅值不能单独给出初始绝对方向，必要时使用“反转”键校正正负方向。

程序刻意不使用当前固件的原生速度控制开关：实机测试表明该开关的行为与配套 PDF、头文件定义不一致。由软件按时间累计纳米目标，可以限制每一时刻的累计位移，避免小段目标被高速重复发送。

## 当前验证状态

- 已通过 64 位严格编译和离线运动参数测试。
- 已通过正弦计划参数、四分之一周期关键点、完整周期回零和峰值速度上限的离线测试。
- 已确认程序可在不连接仪器时启动，并能加载项目内的 `NTControl.dll`。
- 已通过只读实机探针确认设备连接、通道 0 传感器状态和位置读取接口正常。
- 已实机确认连接后保持待机时，当前位置会每 `100 ms` 自动读取并刷新，不依赖运动指令。
- 已通过离线测试确认 Ametek 八字段数据解析、默认参数位移计算、`0 / π/2` 边界展开、远离边界的真实换向和异常数据拒绝逻辑。
- 已实机验证长按后退约 1 秒得到约 `-20 nm`，长按前进约 1 秒得到约 `+14 nm`，运动期间位置读数会刷新。
- 已实机验证 `+100 nm / 5 s` 与 `-100 nm / 5 s` 往返轨迹，实际用时分别约 `5.093 s`、`5.062 s`；往返前后位置读数相差约 `1 nm`。
- 已通过正式图形界面和正式正弦运动线程完成实机验证：默认 `100 nm / 1 Hz / 5 s` 轨迹实测峰峰范围约 `204 nm`、用时 `5.071 s`、停止后回差约 `-1 nm`；两次 `20 nm / 0.5 Hz / 4 s` 参数写入与读回一致，实际用时均约 `4.074 s`，实测峰峰范围均约 `48 nm`，停止后的相对起点读数分别为 `-7 nm` 和 `+9 nm`。全过程未出现控制器故障或限位状态。
- 当前编码器连续待机读数实测在约 `7 nm` 的范围内波动，因此小振幅正弦测试采用 `±10 nm` 回零容差。可复现探针位于 `tests/gui_sine_hardware_test.c`；它会真实驱动位移台，刻意不纳入日常 `build.bat`。

当前控制器报告“物理位置未知”，且软件位置限制为 `0 / 0`（未配置）。界面位置是传感器相对坐标，不应当作已经寻零的绝对位置。启动、停止、传感器噪声和基线漂移会造成测得位移与指令值存在少量差异。使用时仍应保证机构远离物理限位。

## 重新构建

双击 `build.bat`。构建脚本使用现有的 64 位 MinGW-W64，并自动运行离线测试。源代码位于 `src`，测试位于 `tests`。
